import {parkingPositions,parkingStyle,parkingExplanation} from './parking-display.js?v=open-parking-1';
export function parkingAt(map,x,y){return (map.parking_spaces||[]).find(p=>{const a=p.yaw*Math.PI/180,dx=x-p.x,dy=y-p.y;return Math.abs(dx*Math.cos(a)+dy*Math.sin(a))<=p.length/2&&Math.abs(-dx*Math.sin(a)+dy*Math.cos(a))<=p.width/2})}
export function parkingInspectAt(map,x,y){return parkingAt({parking_spaces:parkingPositions(map)},x,y)}
export function drawParking(ctx,map,occupied={},selected=null,scale=1,light=false){
 for(const p of parkingPositions(map)){const active=p.id===selected,style=parkingStyle(p,!!occupied[p.id],light);ctx.beginPath();p.polygon.forEach((v,i)=>i?ctx.lineTo(...v):ctx.moveTo(...v));ctx.closePath();ctx.fillStyle=style.fill;ctx.fill();ctx.strokeStyle=active?(light?'#222222':'#ffffff'):style.color;ctx.lineWidth=(active?2.5:1)/scale;ctx.setLineDash([]);ctx.stroke();ctx.font=`bold ${Math.min(16,Math.max(9,scale*1.5))/scale}px sans-serif`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillStyle=style.color;ctx.fillText(active?p.id:'P',p.x,p.y)}
}
export class ParkingPanel{
 constructor(root,command,notice,onChange,onActor){this.root=root;this.command=command;this.notice=notice;this.onChange=onChange;this.onActor=onActor;this.selected=null;
  this.bays=root.querySelector('#parking-bay');this.models=root.querySelector('#parking-model');this.button=root.querySelector('#parking-spawn');
  this.bays.onchange=()=>this.select(this.bays.value);
  this.button.onclick=async()=>{try{const r=await command('spawn',{role:'background',model:this.models.value,parking_space:this.selected});onActor(r.id);notice(`Vehicle ${r.id} parked in ${r.parking_space}. Handbrake applied; autopilot is off.`,true)}catch(e){notice(e.message)}};
 }
 select(id){this.selected=id||null;this.bays.value=this.selected||'';this.renderDetail();this.onChange()}
 render(state,map,catalog){this.state=state;this.map=map;const surveyLink=this.root.querySelector('#parking-survey-link');if(surveyLink)surveyLink.hidden=map.name?.split('/').pop()!=='Town10HD_Opt';const spaces=map.parking_spaces||[],occupied=state.parking?.occupied||{};
  this.root.querySelector('#parking-count').textContent=`${spaces.length} mapped positions · ${spaces.filter(p=>!occupied[p.id]&&!state.parking?.reserved?.[p.id]).length} open · ${spaces.filter(p=>occupied[p.id]||state.parking?.reserved?.[p.id]).length} occupied`;
  const keys=JSON.stringify(spaces.map(p=>p.id));if(keys!==this.keys){this.keys=keys;this.bays.replaceChildren(new Option('Select a bay on the map or here',''),...spaces.map(p=>new Option(p.id,p.id)));if(!parkingPositions(map).some(p=>p.id===this.selected))this.selected=null;this.bays.value=this.selected||''}
  for(const option of this.bays.options)if(option.value)option.textContent=`${option.value} · ${occupied[option.value]||state.parking?.reserved?.[option.value]?'Occupied':'Open'}`;
  const vehicles=(catalog.vehicles||[]).filter(v=>v.id.startsWith('vehicle.'));const modelKey=vehicles.map(v=>v.id).join(',');if(this.modelKey!==modelKey){this.modelKey=modelKey;const old=this.models.value;this.models.replaceChildren(...vehicles.map(v=>new Option(v.label,v.id)));this.models.value=vehicles.some(v=>v.id===old)?old:vehicles.find(v=>v.id==='vehicle.mini.cooper')?.id||vehicles.find(v=>v.id==='vehicle.tesla.model3')?.id||vehicles[0]?.id||''}
  this.renderDetail();
 }
 renderDetail(){const p=parkingPositions(this.map||{}).find(p=>p.id===this.selected),used=this.state?.parking?.occupied?.[this.selected]||(this.state?.parking?.reserved?.[this.selected]?{kind:'actor',id:this.state.parking.reserved[this.selected]}:null);this.root.querySelector('#parking-detail').textContent=p?`${p.id} · ${p.length?`${p.length.toFixed(1)} × ${p.width.toFixed(1)} m`:'Curb parking area'} · ${parkingStyle(p,!!used).label}${used?.kind==='actor'?' · vehicle '+used.id:''}. ${parkingExplanation(p)} ${p.boundary_note||'Individual position boundaries are estimated.'}`:'Each solid rectangle with P is an individual mapped parking position. Purple: open. Grey: occupied. All surveyed curb positions are displayed.';this.setDisabled(this.disabled)}
 setDisabled(disabled){this.disabled=disabled;this.button.disabled=!!disabled||!this.selected||!this.map?.parking_spaces?.some(p=>p.id===this.selected)||!!this.state?.parking?.occupied?.[this.selected]||!!this.state?.parking?.reserved?.[this.selected];this.root.querySelector('#parking-help').textContent=this.state?.running?'Pause to place or remove parked vehicles. Parking bays remain selectable during a run.':'Place a background vehicle in a free bay, then select it and choose a road destination or another parking bay. It drives parking maneuvers at low speed and uses Traffic Manager on the road.'}
}
